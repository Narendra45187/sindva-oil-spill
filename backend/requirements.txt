fastapi
uvicorn[standard]
python-multipart
# Pinned: newer rasterio wheels bundle a GDAL DLL with a per-build hashed
# filename that has no reputation history, which Windows Smart App Control
# blocks on load (ImportError: DLL load failed ... Application Control
# policy). 1.3.10 is a long-established build that is already trusted.
rasterio==1.3.10
opencv-python-headless
numpy
pandas
requests
# ML detection (backend/detection/ml_detect.py) -- CPU inference only, no
# GPU/CUDA needed. Installed locally via the PyTorch CPU wheel index:
#   py -3.12 -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# (plain `pip install torch` also works, but pulls PyTorch's much larger
# default CUDA build; use the command above to get the small CPU-only one.)
torch
torchvision
# Loads uploaded images (png/jpg/jpeg/tif/tiff) for the ML detection path
# exactly as training did -- already installed transitively via
# torchvision, declared directly here since ml_detect.py imports it itself.
Pillow
