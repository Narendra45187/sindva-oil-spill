fastapi
uvicorn[standard]
python-multipart
# Pinned: newer rasterio wheels bundle a GDAL DLL with a per-build hashed
# filename that has no reputation history, which Windows Smart App Control
# blocks on load (ImportError: DLL load failed ... Application Control
# policy). 1.3.10 is a long-established build that is already trusted.
rasterio==1.3.10
opencv-python-headless
numpy
pandas
requests
